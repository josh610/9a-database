type Climber {
  _id: ID!
  name: String!
  nickname: String
  dob: Date
  sex: String
  country: Country
  ascents: [Ascent!]
  fas: [Ascent]
  description: String
  addedAt: Date!
  updatedAt: Date!
}

input ClimbersFilter {
  name: String
  nickname: String
  country: String
}

input CreateClimberInput {
  name: String!
  nickname: String
  dob: String
  sex: String
  country: ID
  description: String
}

type Query {
  climber(ID: ID!): Climber
  climbers(filter: ClimbersFilter): [Climber!]
}

type Mutation {
  createClimber(input: CreateClimberInput!): Climber!
  deleteClimber(ID: ID!): Boolean!
}
